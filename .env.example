# Optional Google Gemini API Key (If omitted, system uses deterministic offline engine)
GEMINI_API_KEY=
GOOGLE_API_KEY=

# Model choice
GEMINI_MODEL_NAME=gemini-2.5-flash

# Force local deterministic engine for testing
FORCE_LOCAL_FALLBACK=false
